let comprimento: number = 10;
let largura: number = 10;
let altura: number = 10;

let fVolume = (comp:number, larg:number, alt:number) => (comp * larg * alt)

console.log(`${fVolume(comprimento,largura,altura)}`);